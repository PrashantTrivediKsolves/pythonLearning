import sys
import os

# Add the parent directory (PYTHONLEARNING) to the Python path
sys.path.append(os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))

# Now import the file 
import evaluate_standardized_street_name


